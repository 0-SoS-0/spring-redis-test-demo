spring:
  redis:
    host: 192.168.204.129
    port: 6379
    password: 123456
    lettuce:
      pool:
        max-active: 8   # 最大连接数，即让创建的 redis实例数
        max-idle: 8     #最大空闲连接
        min-idle: 0     #最小空闲连接，在经过指定的等待时间以后能够存在的连接数
        max-wait: 100   #等待时间
